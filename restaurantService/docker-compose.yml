version: '3'
services:
  mongodb:
    image: mongo:latest
    container_name: restaurant_mongodb
    ports:
      - "27018:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=restaurant_db

volumes:
  mongodb_data: